import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.allure.calculator',
  appName: 'Allure Calculator',
  webDir: 'dist'
};

export default config;
